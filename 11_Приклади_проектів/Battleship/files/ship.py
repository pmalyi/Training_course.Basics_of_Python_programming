"""
Модуль містить перелік Direction та клас Ship.

Ship - це, у цьому контексті, складений (композитний) набір об'єктів
Part, з яких і складається корабель.
"""

from enum import Enum

from part import Part


class Direction(Enum):
    """
    Перелік Direction показує, у якому напрямку спрямований корабель.
    """

    NORTH = 0  # вгору
    EAST = 1  # праворуч
    SOUTH = 2  # вниз
    WEST = 3  # ліворуч


class Ship:
    """
    Клас Ship - набір об'єктів Part.

    Цей клас представляє корабель для гри Battleship. Корабель
    складається з частин, тобто об'єктів Part, які зберігаються
    у списку (list).

    Кораблі різних розмірів представлені різною кількістю частин:
      - "Лінкор" (Dreadnought) має 5 частин;
      - "Крейсер" (Cruiser) має 4 частини;
      - "Есмінець" (Destroyer) має 3 частини;
      - "Підводний човен" (Submarine) має 2 частини.
    """

    def __init__(self):
        """
        Стандартний (порожній) конструктор класу Ship.

        Не повинен нічого робити, окрім ініціалізації порожнього
        списку частин. Потрібен для того, щоб можна було заздалегідь
        створити список кораблів (наприклад, у класі GameBoard),
        а розмістити їх на полі - пізніше, викликом place().
        """
        self._parts: list[Part] = []

    def build(self, row: int, col: int, length_of_ship: int,
              direction: Direction) -> None:
        """
        Розміщує (формує) корабель на полі.

        :param row: рядок, де буде розташована перша (кормова)
            частина корабля.
        :param col: стовпець, де буде розташована перша (кормова)
            частина корабля.
        :param length_of_ship: довжина корабля (зазвичай від 2 до 5).
        :param direction: напрямок, у якому спрямований корабель.
        :raises ValueError: якщо напрямок є некоректним, або якщо
            частини корабля виходять за межі поля 10x10.

        Створює корабель із заданими параметрами. Кормова частина
        корабля завжди розташовується в заданих рядку та стовпці.
        Далі, у заданому напрямку, розміщуються (length_of_ship - 1)
        додаткових частин.

        Наприклад, для row=3, col=2, довжини 3 та direction=EAST
        будуть створені такі частини:
          - Part на позиції (3, 2)
          - Part на позиції (3, 3)
          - Part на позиції (3, 4)
        """
        # TODO: реалізувати
        raise NotImplementedError

    def has_part_in(self, row: int, col: int) -> bool:
        """
        Перевіряє, чи має цей корабель частину в заданій клітинці.

        :param row: рядок клітинки, що перевіряється.
        :param col: стовпець клітинки, що перевіряється.
        :return: True, якщо частина цього корабля знаходиться
            в клітинці (row, col), False - в іншому випадку.

        Ця функція використовується, щоб визначити, чи присутній
        корабель у заданій клітинці. Для цього потрібно перевірити
        список частин _parts на наявність частини з такою позицією.

        Див. також: get_part_in
        """
        # TODO: реалізувати
        raise NotImplementedError

    def get_part_in(self, row: int, col: int) -> Part:
        """
        Повертає частину корабля, що розташована в заданій клітинці.

        :param row: рядок частини.
        :param col: стовпець частини.
        :return: об'єкт Part у заданій позиції.
        :raises ValueError: якщо такої частини не існує.

        Рекомендується викликати цей метод, лише якщо ви впевнені,
        що задана позиція дійсно містить частину цього корабля,
        наприклад, попередньо викликавши has_part_in.

        Див. також: has_part_in
        """
        # TODO: реалізувати
        raise NotImplementedError

    def is_damaged(self) -> bool:
        """
        Повертає, чи пошкоджений цей корабель.

        :return: True, якщо корабель пошкоджений.

        Корабель вважається пошкодженим, якщо пошкоджена хоча б
        одна з його частин. Якщо пошкоджені ВСІ частини, корабель
        вважається затонулим, що також рахується як пошкоджений.

        Див. також: is_sunk
        """
        # TODO: реалізувати
        raise NotImplementedError

    def is_sunk(self) -> bool:
        """
        Повертає, чи затонув цей корабель.

        :return: True, якщо корабель затонув.

        Корабель вважається затонулим, якщо пошкоджені ВСІ його
        частини.

        Див. також: is_damaged
        """
        # TODO: реалізувати
        raise NotImplementedError
