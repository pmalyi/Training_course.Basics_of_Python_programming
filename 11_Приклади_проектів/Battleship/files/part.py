"""
Модуль містить клас Part.

Клас Part представляє одну частину корабля (Ship), що займає
рівно одну клітинку ігрового поля.
"""


class Part:
    """
    Клас Part представляє частину корабля в грі Battleship.

    Один об'єкт Part розташований у конкретній позиції, що задається
    рядком (row) та стовпцем (col). Декілька об'єктів Part складають
    один об'єкт Ship (відношення "композиція").

    Частина корабля має статус, який показує, пошкоджена вона чи ні.
    Частина корабля не "знає", якому кораблю вона належить,
    і не знає, чи затонув цей корабель.
    """

    def __init__(self, row: int, col: int):
        """
        Конструктор класу Part.

        :param row: рядок сітки, на якому розташована ця частина.
        :param col: стовпець сітки, на якому розташована ця частина.

        Це єдиний конструктор класу. Значення атрибутів _row та _col
        присвоюються з параметрів row та col відповідно. Статус
        _status ініціалізується значенням, що відповідає
        "не пошкоджено".
        """
        self._row: int = row
        self._col: int = col
        self._status: int = 0  # 0 - не пошкоджено, 1 - пошкоджено

    def is_damaged(self) -> bool:
        """
        Повертає, чи пошкоджена ця частина корабля.

        :return: True, якщо частина пошкоджена, False - якщо ні.

        Окремого статусу для затонулого корабля немає. За визначенням,
        корабель вважається затонулим, коли пошкоджені всі його частини.
        """
        # TODO: реалізувати
        raise NotImplementedError

    def set_damaged(self) -> None:
        """
        Встановлює статус цієї частини як "пошкоджено".

        Атрибут _status дорівнює 0, якщо пошкоджень немає, і 1,
        якщо цю частину було пошкоджено. Цей метод встановлює
        _status у значення 1. "Ремонт" (повернення статусу
        "не пошкоджено") не передбачений.
        """
        # TODO: реалізувати
        raise NotImplementedError

    def get_row(self) -> int:
        """
        Повертає рядок, із яким було створено цю частину.

        :return: рядок розташування частини.
        """
        # TODO: реалізувати
        raise NotImplementedError

    def get_col(self) -> int:
        """
        Повертає стовпець, із яким було створено цю частину.

        :return: стовпець розташування частини.
        """
        # TODO: реалізувати
        raise NotImplementedError
